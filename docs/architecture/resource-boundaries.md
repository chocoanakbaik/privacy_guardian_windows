# Resource Boundaries

JSON, schemas, lexicons, rule definitions, localization, templates, and configuration are externalized resources. Their loading, validation, versioning, and compatibility must be handled explicitly by implementation code.

Resource files define data and configuration boundaries; they do not replace the C# application architecture.
