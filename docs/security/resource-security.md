# Resource Security

Rule files, configuration, templates, and localization resources are project-controlled inputs. Runtime code must validate resource structure and fail safely when resources are missing, malformed, incompatible, or unexpectedly modified.
